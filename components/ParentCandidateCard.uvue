<template>
	<view class="candidate-card" @click="openDetail">
		<view class="candidate-copy">
			<text class="candidate-name">{{ candidate.displayName }}</text>
			<text class="candidate-summary">{{ candidate.genderText }} · {{ candidate.birthYear }}年 · {{ candidate.city }}</text>
			<text class="candidate-facts">{{ candidate.height }} · {{ candidate.education }} · {{ candidate.job }}</text>
			<text class="candidate-cert">{{ candidate.certificationText }}</text>
		</view>

		<view class="candidate-photo-wrap">
			<view class="candidate-photo-icon">
				<XsaIcon name="profile" size="large" />
			</view>
			<view class="photo-shield"><text>照片已保护</text></view>
		</view>

		<view class="candidate-actions">
			<button class="card-action secondary" @click.stop="toggleLike">
				{{ candidate.liked ? '已喜欢' : '喜欢' }}
			</button>
			<button class="card-action primary" @click.stop="applyToMeet">申请认识</button>
		</view>
	</view>
</template>

	<script setup lang="uts">
		import XsaIcon from '@/components/XsaIcon.uvue'

		interface ParentCandidateView {
			id: number
			displayName: string
			genderText: string
			birthYear: number
			city: string
			height: string
			education: string
			job: string
			certificationText: string
			avatar: string
			liked: boolean
		}

		interface Props {
			candidate: ParentCandidateView
		}

		const props = defineProps<Props>()
		const emit = defineEmits<{
			open: [candidate: ParentCandidateView]
			like: [candidate: ParentCandidateView]
			apply: [candidate: ParentCandidateView]
		}>()

	const openDetail = () => emit('open', props.candidate)
	const toggleLike = () => emit('like', props.candidate)
	const applyToMeet = () => emit('apply', props.candidate)
</script>

<style scoped>
	.candidate-card {
		display: grid;
		grid-template-columns: minmax(0, 1fr) 128px;
		gap: 16px;
		padding: 18px;
		background: var(--surface);
		border: 1px solid var(--line);
		border-radius: 18px;
		box-shadow: var(--shadow-sm);
	}

	.candidate-copy {
		min-width: 0;
		display: flex;
		flex-direction: column;
		justify-content: center;
		gap: 8px;
	}

	.candidate-name {
		font-size: 20px;
		font-family: var(--sans);
		font-weight: 800;
		line-height: 1.3;
		color: var(--ink);
	}

	.candidate-summary,
	.candidate-facts {
		font-size: 14px;
		line-height: 1.5;
		color: var(--ink2);
	}

	.candidate-cert {
		font-size: 13px;
		font-weight: 700;
		line-height: 1.45;
		color: var(--sage);
	}

	.candidate-photo-wrap {
		position: relative;
		min-height: 156px;
		border-radius: 14px;
		overflow: hidden;
		background: var(--warm);
		display: flex;
		align-items: center;
		justify-content: center;
	}

	.candidate-photo-icon {
		color: var(--accent2);
		opacity: 0.72;
		display: flex;
		align-items: center;
		justify-content: center;
	}

	.photo-shield {
		position: absolute;
		left: 8px;
		right: 8px;
		bottom: 8px;
		min-height: 32px;
		padding: 4px 8px;
		border-radius: 10px;
		background: var(--paper);
		color: var(--ink2);
		display: flex;
		align-items: center;
		justify-content: center;
		font-size: 12px;
		font-weight: 700;
		text-align: center;
	}

	.candidate-actions {
		grid-column: 1 / -1;
		display: grid;
		grid-template-columns: 1fr 1.4fr;
		gap: 10px;
	}

	.card-action {
		min-height: 48px;
		margin: 0;
		border-radius: 12px;
		font-size: 14px;
		font-weight: 700;
		line-height: 48px;
	}

	.card-action.secondary {
		background: var(--paper);
		border: 1px solid var(--line);
		color: var(--ink);
	}

	.card-action.primary {
		background: var(--accent);
		color: var(--text-invert);
	}
</style>
