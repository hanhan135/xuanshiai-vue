<template>
	<view class="rank-wrap">
		<view class="rank-header">
			<view>
				<text class="rank-title">红娘排行榜</text>
				<text class="rank-subtitle">按已完成服务数量排序</text>
			</view>
		</view>

		<view class="rank-list">
				<view class="rank-item" v-for="(item, index) in list" :key="item.id" @click="handleClick(item)">
					<view class="rank-avatar-wrap">
						<MatchmakerAvatar :src="item.avatar" :name="item.name" variant="rank" />
						<view class="rank-mark" :class="rankClass(index)">
							<text>{{ rankLabel(index) }}</text>
						</view>
					</view>
					<text class="rank-name">{{ item.name }}</text>
					<text class="rank-meta">服务 {{ item.serviceCount }} 人</text>
					<text class="rank-meta">成功 {{ item.successCount }} 约</text>
				</view>
		</view>
	</view>
</template>

<script setup lang="uts">
import MatchmakerAvatar from '@/components/MatchmakerAvatar.uvue';

interface Props {
	list: any[];
}

const props = defineProps<Props>();

const emit = defineEmits<{
	'item-click': [item: any];
}>();

const rankLabel = (index: number) => {
	return String(index + 1);
};

const rankClass = (index: number) => {
	if (index == 0) return 'first';
	if (index == 1) return 'second';
	if (index == 2) return 'third';
	return 'normal';
};

const handleClick = (item: any) => {
	emit('item-click', item);
};
</script>

<style scoped>
	.rank-wrap {
		padding: 28rpx 24rpx 24rpx;
		border-radius: 18rpx 34rpx 18rpx 28rpx;
		background: var(--match-paper);
		box-shadow: var(--match-shadow);
		border: 1px solid var(--match-line);
	}

	.rank-header {
		margin-bottom: 20rpx;
	}

	.rank-title {
		display: block;
		font-size: 34rpx;
		line-height: 1.2;
		font-family: var(--serif);
		font-weight: 700;
		color: var(--ink);
	}

	.rank-subtitle {
		display: block;
		margin-top: 8rpx;
		font-size: 22rpx;
		color: var(--muted);
	}

	.rank-list {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 18rpx;
		width: 100%;
	}

	.rank-item {
		width: 100%;
		min-width: 0;
		display: flex;
		flex-direction: column;
		align-items: center;
		text-align: center;
	}

	.rank-avatar-wrap {
		position: relative;
		width: 116rpx;
		height: 116rpx;
	}

	.rank-mark {
		position: absolute;
		right: -8rpx;
		bottom: -8rpx;
		min-width: 44rpx;
		height: 44rpx;
		padding: 0 10rpx;
		border-radius: 999rpx;
		display: flex;
		align-items: center;
		justify-content: center;
		font-size: 18rpx;
		font-weight: 800;
		background: var(--match-paper);
		color: var(--match-deep);
		border: 3rpx solid var(--match-paper);
		box-shadow: var(--shadow-sm);
	}

	.rank-mark.first {
		background: var(--match-deep);
		color: var(--text-invert);
	}

	.rank-mark.second {
		background: var(--match-sky);
		color: var(--match-deep);
	}

	.rank-mark.third {
		background: var(--match-blush);
		color: var(--match-coral);
	}

	.rank-name {
		max-width: 100%;
		margin-top: 14rpx;
		font-size: 26rpx;
		line-height: 1.3;
		font-family: var(--serif);
		font-weight: 700;
		color: var(--ink);
		overflow: hidden;
		text-overflow: ellipsis;
		white-space: nowrap;
	}

	.rank-meta {
		max-width: 100%;
		margin-top: 8rpx;
		font-size: 20rpx;
		line-height: 1.4;
		color: var(--muted);
		overflow: hidden;
		text-overflow: ellipsis;
		white-space: nowrap;
	}

</style>
