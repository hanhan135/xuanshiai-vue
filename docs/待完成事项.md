# 待完成事项

## 媒体上传后再发布动态

状态：已接入（前端）。

发布页与纸飞机页已在选择媒体后调用 `uploadCommunityMedia`，发布/放飞仅传 `imageMediaIds` / `videoMediaId`；取消或卸载时对未绑定的 ready 媒体调用 `deleteCommunityMedia`。真实环境仍依赖生产 HTTPS 域名与小程序合法域名配置；局域网开发地址不能作为生产域名。

## 微信小程序端侧回归
状态：已完成（本地 DevTools 模拟器）。

2026-07-30 使用 HBuilderX 5.15 编译 `mp-weixin` 成功，产物为 `unpackage/dist/dev/mp-weixin`，并使用 AppID `wxb5f4e639f4eb2591` 导入微信开发者工具。冷启动、调试登录、首页进入社区、话题/活动/通知/纸飞机页面和社区真实 HTTP 路径均已回归通过。

证据：`docs/PROJECT_STATUS.md` 和 `unpackage/smoke-shots/`。本地 DevTools 通过不等于物理真机、正式 HTTPS 合法域名或生产发布验收。

## AI 画像（M04）
状态：前端已实现。

`pagesSub/profileExtra/my-portrait.uvue`、`api/ai-profile.uts` + `mock/ai-profile.uts`、组件 `XsaPortraitField` / `VoiceRecorder` / `VoiceWaveform` 已落地，支持文字 / 语音双模式对话、字段逐项确认 / 修改 / 拒绝 / 删除、发布版本与历史恢复；`tests/test-ai-profile-page.js` 契约测试通过。待办：真实 ASR 接入、后端 `/ai/*` 接口联调、67% 提前建构、暂停 / 恢复 / 重新开始交互。

## 墨相师双画像建构（2026-08-31）

状态：代码与 HBuilderX 编译已完成，端侧业务闭环被开发 API 旧地址阻塞，待切换配置后验收。

- 已实现同一聊天时间线中的「我的墨相」/「愿遇之相」分阶段切换，并分别保存进度、门槛、草稿版本和确认卡片；「我的」页已收敛为唯一墨相师建构入口，画像页只在显式备用参数下进入旧流程。
- HBuilderX 已成功重新生成 `unpackage/dist/dev/mp-weixin`，微信开发者工具已成功打开该产物；官方自动化通道已能读取路由栈，但页面 DOM 查询在当前 UTS/DevTools 组合上超时，因此登录态、切换、卡片确认、分别发布、断线重连和主动备用入口仍不得标记为真机通过。
- 当前 WLAN IPv4 为 `10.5.163.243`，而用户已有 `api/config.uts` 仍配置 `192.168.112.65:8000`；旧地址健康检查超时，新地址健康检查返回 200。该用户配置未被本任务擅自覆盖，联调前需确认网络后更新开发地址并重新编译。
- `npm run verify:mp:dev` 已能正确调用共享审计脚本，但当前产物主包约 11.4 MiB，超过项目 3 MiB 开发门槛；另有 13 个媒体文件超过 200 KiB。上述为存量资源/分包问题，本功能未新增这些媒体，发布前必须单独优化并重新审计。

## 墨相师四阶段复验（2026-09-01）

状态：自动化、包体与 HBuilderX 编译已通过；微信开发者工具已登录并完成路由烟测，真实业务闭环仍待修正开发 API 地址后执行。

- 将生产页面引用的超大 PNG/JPEG 转为 WebP；压缩前原图备份已按用户要求移入回收站，页面引用和构建产物不再携带这些原图。
- `location.json` 已移出 `static/` 并改为代码数据导入，避免整份原始 JSON 被复制进微信主包。
- 新鲜 `npm run verify:mp` 通过：主包约 `2031 KiB / 2048 KiB`，6 个分包全部通过，23 个媒体文件均低于 200 KiB；主包仅剩约 17 KiB 余量，后续新增资源仍需持续审计。
- `tests/test-moxiang-master-page.js` 与 `tests/test-mock-system.js` 均通过；HBuilderX 5.15 新鲜编译 60 个页面成功。未关联 uniCloud 的提示不影响本次本地编译结论。
- 微信开发者工具已登录并成功导入产物；官方自动化直连可读取首页并完成墨相师主流程三个页面的路由入栈/回退烟测，未捕获 JS exception。由于 `api/config.uts` 仍指向用户已有的 `192.168.112.65:8000`，实际 `/ai/*` 请求在模拟器中超时；当前后端可达地址为 `10.5.163.243:8000`。该配置属于保护文件，本轮未擅自覆盖；切换到可达地址并重新编译后，才能继续登录态、双主体切换、确认、分别发布、断线重连、Console 与 Network 的业务验收。
